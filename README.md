### Проведённый рефакторинг

Сделал типы черкз enum

В Animal теперь только данные, вынес логику в AnimalHelper (SPR - данные в одном месте, логика в другом)

Убрал повторяющиеся методы, теперб только по одному методу "взаимодействия с животными", которые выполняют работу в зависимости от полученного типа

Переименовал методы, чтоб было понятнее, что они делают (RunZoo теперь SimulateZooTour, а DoSomethingWithAnimals стал PerformZooOperation)

SimulateZooTour (ранее RunZoo) теперь выполняет несколько методов, что легче читать

В методе для звуков животных теперб только два кейса - если для типа жтвотного звук есть, и если его нет

Теперь легче расширять код, достаточно добавить желаемый тип
